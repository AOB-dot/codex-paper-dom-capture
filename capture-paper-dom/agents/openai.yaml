interface:
  display_name: "Capture Paper DOM"
  short_description: "Capture live DOM sections into editable Paper frames"
  default_prompt: "Use $capture-paper-dom to capture the selected browser section into editable Paper frames across laptop, mobile, and tablet."
policy:
  allow_implicit_invocation: true
